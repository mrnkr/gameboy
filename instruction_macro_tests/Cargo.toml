[package]
name = "instruction_macro_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
instruction_macro = { path = "../instruction_macro" }
inventory = "0.3"
once_cell = "1.21"
rstest = "0.18"
